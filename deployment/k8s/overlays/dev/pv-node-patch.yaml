apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv

spec:
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - minikube